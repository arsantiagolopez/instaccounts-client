import React from "react";
import { Layout } from "../Layout";

interface Props {}

const Dashboard: React.FC<Props> = () => {
  return (
    <Layout>
      <div>Howdy</div>
    </Layout>
  );
};

export { Dashboard };

// Styles

const styles: any = {
  wrapper: {},
};
